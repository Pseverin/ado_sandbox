trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  a: 'a'
  b_b: 'b_b'
  c-c: 'c-c'

steps:
  - task: Bash@3
    description: ${{ variables.a }}
    inputs:
      targetType: 'inline'
      script: |
        echo ${{ variables.a }}
  - task: Bash@3
    description: $(a)
    inputs:
      targetType: 'inline'
      script: |
        echo $(a)